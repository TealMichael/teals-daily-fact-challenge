# Resilience pass: keep classroom deployments on known, pre-change dependency lines
# instead of silently moving to a brand-new Streamlit/Supabase release on rebuild.
streamlit==1.61.1
supabase==2.28.3
pandas>=2.1,<3.0
