TEAL'S DAILY FACT CHALLENGE v2.12.0 — HOTFIX 3 CLASSROOM SAFETY + TOP 10 CHIME

BASE
This combined patch is for the currently installed v2.12.0 Hotfix 1 AWTRIX Live Fix build.
It includes the not-yet-installed Hotfix 2 Teacher Today API-resilience protection plus the Top 10 chime.

STEP 1 — SUPABASE
No new SQL for Hotfix 3.
Keep the v2.12.0 and AWTRIX Hotfix 1 database changes already applied.
Do NOT rerun older migrations for this patch.

STEP 2 — GITHUB / STREAMLIT
Copy the contents of COPY_INTO_GITHUB_REPO (patch package) or UPLOAD_TO_GITHUB (full archive) into the repository, replacing existing files, then commit and push.
Commit message:
  v2.12.0 Hotfix 3 - restore Teacher Today resilience and add Top 10 chime

STEP 3 — AWTRIX CLOCK
Update ONLY the Fact Top 10 script. Keep the Class Schedule script unchanged.
1. Open the AWTRIX web UI.
2. Open Scripts and select the installed Fact Top 10 script.
3. Replace its source with AWTRIX_FactTop10.berry from this package (or import/copy it into the editor under the same app name).
4. Save/install it. Keeping the same app preserves the script's stored configuration on the device.
5. Confirm the global AWTRIX Sound setting is enabled.
6. Use Teacher > Clock > Send Top 10 to Clock Now for a manual test.

The sound is a single three-note RTTTL chime when the Top 10 notification first appears. It does not loop or play before each nickname.
