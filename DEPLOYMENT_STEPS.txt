TEAL'S DAILY FACT CHALLENGE v2.11.2 — DATA TRUST PASS

IMPORTANT: this patch adds a small backward-compatible Supabase migration.

1. In Supabase SQL Editor, run the entire file:
   RUN_THIS_ONCE_IN_SUPABASE_v2_11_2_DATA_TRUST.sql

2. Confirm the SQL finishes successfully. The currently installed v2.11.2 app remains compatible after this migration, so there is no need to rush the GitHub push.

3. Copy the patch files into your cloned GitHub repository using GitHub Desktop workflow.

4. Commit with:
   v2.11.2 - strengthen Daily and Fact Fluency data trust

5. Push origin when ready for Streamlit to redeploy.

No new Streamlit Secret is required.
Do not rerun older Supabase migrations.
