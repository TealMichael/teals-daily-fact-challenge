TEAL'S DAILY FACT CHALLENGE v2.11.0.1
STARTUP RESILIENCE PASS

IMPORTANT BASE:
- This release is built directly on v2.11.0.
- v2.11.1 Stability/Foundation is intentionally NOT included yet.
- This package changes only Teal's Daily Fact Challenge, not Yahtzee.

UPDATING FROM v2.11.0:
1. There is NO new Supabase SQL migration.
2. There is NO new Streamlit Secret.
3. Upload/replace every file and folder in this package's UPLOAD_TO_GITHUB directory to the existing GitHub repository root.
4. Let Streamlit perform a full redeploy so the pinned dependency versions are installed.
5. Do not separately install the paused v2.11.1 package.

QUICK CHECK AFTER DEPLOYMENT:
- Open the app on a device with a remembered student login. The Teal's Daily Fact Challenge shell should appear before backend restore work completes.
- If saved-login restoration hits a temporary connection failure, the app should offer "Try saved sign-in again" instead of deleting the remembered login.
- Student Daily/Teacher pages should otherwise look and behave like v2.11.0.
- If an intermittent spinner occurs, check Streamlit logs for lines beginning with: [TDFC timing]

NO SQL TO RUN.
NO NEW SECRET.

Suggested GitHub commit:
v2.11.0.1 - add startup and Supabase resilience
