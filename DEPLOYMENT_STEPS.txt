TEAL'S DAILY FACT CHALLENGE v2.2.5
CLASSROOM LOAD RELIABILITY HOTFIX — EXISTING LIVE v2.2 APP

IMPORTANT: This is a CODE-ONLY hotfix.
NO Supabase SQL step is required.
Do NOT rerun v2, v2_1, v2_2, or SUPABASE_SCHEMA.sql.

YOUR CURRENT PATH
1. Upload EVERY file/folder inside this package's UPLOAD_TO_GITHUB folder to the existing GitHub repo root.
2. Replace matching files.
3. Confirm daily_sprint_component/index.html is still present.
4. Commit the upload.
5. Streamlit should redeploy automatically.

Suggested commit:
   v2.2.5 - improve classroom load reliability

WHAT CHANGED
- Retries transient Supabase/httpx ReadError-style failures automatically.
- Batches Daily mastery writes so 10 facts no longer create ~20 mastery requests per student.
- Loads the completed-screen leaderboard once per rerun instead of repeatedly.
- Replaces post-Daily red tracebacks with a friendly Try again state when the classroom connection is briefly busy.
- A completed Daily remains saved; students should not redo their 10 facts after a retry screen.

UNCHANGED
- Daily fact generator and ranking rules
- student leaderboard privacy (rank + nickname only)
- Fix Your Misses / adaptive Focus Practice
- Weekly Mystery
- roster management / visible teacher PINs
- Teacher Tools layout (cleanup still deferred)
- Supabase schema
- Streamlit Secrets
