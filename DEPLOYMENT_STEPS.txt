TEAL'S DAILY FACT CHALLENGE v2.2.6
FOCUS PRACTICE SPEED HOTFIX — EXISTING LIVE v2.2 APP

IMPORTANT: This is a CODE-ONLY hotfix.
NO Supabase SQL step is required.
Do NOT rerun v2, v2_1, v2_2, or SUPABASE_SCHEMA.sql.

YOUR CURRENT PATH
1. Upload EVERY file/folder inside this package's UPLOAD_TO_GITHUB folder to the existing GitHub repo root.
2. Replace matching files.
3. Confirm daily_sprint_component/index.html is still present.
4. Commit the upload.
5. Streamlit should redeploy automatically.

Suggested commit:
   v2.2.6 - speed up Focus Practice under classroom load

WHAT CHANGED
- Stops reloading the Top 10 after every Focus answer; one snapshot is reused until Day Complete.
- Caches Focus activity history and teacher-focus settings during the student's current session.
- Reuses already-loaded learning progress when preparing the Focus plan.
- Saves a normal first-try Focus answer with one insert instead of a pre-read plus insert.
- Applies the eight Focus mastery updates in one safe batch at the end instead of updating mastery after every answer.

UNCHANGED
- Daily fact generator and ranking rules
- Fix Your Misses and Focus Practice selection logic
- Weekly Mystery
- roster management / visible teacher PINs
- Teacher Tools layout (cleanup still deferred)
- Supabase schema
- Streamlit Secrets
