TEAL'S DAILY FACT CHALLENGE v2.9.1

PERFORMANCE + STUDENT CLARITY PASS

UPDATING FROM v2.9.0

1. No Supabase SQL is required for v2.9.1.
2. Upload every file/folder in UPLOAD_TO_GITHUB to the existing GitHub repository root, replacing the old files.
3. Let Streamlit redeploy automatically.
4. Student check: the top navigation should read Today | Practice, with a smaller Teacher button.
5. Student check: the routine strip should show Mystery Reward instead of a required Step 4.
6. Teacher check: open Teacher Dashboard and switch between Today, Classes & Rosters, Mastery & Focus, Weekly Mystery, Student Support, and Test Student. Only the selected section should load.
7. Teacher → Today: Refresh data, Top 10, PINs, routine status, and teacher-only accuracy/timing should still work normally.
8. Optional Practice → My Focus Facts: move through several facts and confirm question-to-question use remains fast.

If this installation is still on v2.8.x and RUN_THIS_ONCE_IN_SUPABASE_v2_9.sql has never been run, run that v2.9 migration once before deploying v2.9.1. Do NOT rerun it if v2.9.0 is already working.

No new Streamlit Secrets are required.

Suggested GitHub commit:
v2.9.1 - speed up teacher dashboard and simplify student UI
